#!/bin/bash

if [[ "$1" = "" ]]
then
    echo "usage: $0 branch"
    exit 1
fi

for i in $(~/bin/lsbr $1|grep '/0$'|sed 's/\/0//g')
do
    #echo removing $i
    cleartool chtype delete_wxmp34 $i
done
