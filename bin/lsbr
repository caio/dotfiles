#!/bin/bash

if [[ "$1" = "" ]]
then
    echo "usage: $0 branch"
    exit 1
fi

export CLEARCASE_AVOBS="/vobs/core_imps"
cleartool find -avobs -nxn -elem "brtype($1)" -exec 'cleartool ls -d -s $CLEARCASE_PN'
